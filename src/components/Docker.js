import './Docker.css'
import dockerImage from '../images/icon.png'

const Docker = () => {
    return (
        <div>
            <img src={ dockerImage } alt='Icon docker'/>
            <h1>I ❤️ Docker!</h1>
        </div>
    )   
}

export default Docker

